import { StyleSheet } from "react-native";

export default StyleSheet.create({
  imageUploadBtn: {
    backgroundColor: "red",
    height: 30,
    width: 30,
    borderRadius: 30,
    justifyContent: "center",
    alignItems: "center",
  },
});
